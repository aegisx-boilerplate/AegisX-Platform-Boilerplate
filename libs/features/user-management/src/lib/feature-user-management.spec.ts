import { featureUserManagement } from './feature-user-management.js';

describe('featureUserManagement', () => {
  it('should work', () => {
    expect(featureUserManagement()).toEqual('feature-user-management');
  });
});
