import { featureFileStorage } from './feature-file-storage.js';

describe('featureFileStorage', () => {
  it('should work', () => {
    expect(featureFileStorage()).toEqual('feature-file-storage');
  });
});
